import { TeamContainer } from '../../../containers/';
import { AppMainLayout } from '../../../layouts';
const TeamPage = () => {
  return (
    <AppMainLayout>
      <TeamContainer />
    </AppMainLayout>
  );
};

export default TeamPage;
