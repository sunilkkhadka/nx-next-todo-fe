import { LicenseContainer } from '../../../containers';
import { AppMainLayout } from '../../../layouts';
const LicensePage = () => {
  return (
    <AppMainLayout>
      <LicenseContainer />
    </AppMainLayout>
  );
};

export default LicensePage;
