import { AboutContainer } from '../../../containers';
import { AppMainLayout } from '../../../layouts';
const About = () => {
  return (
    <AppMainLayout>
      <AboutContainer />;
    </AppMainLayout>
  );
};

export default About;
